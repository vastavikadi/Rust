fn main() {
    println!("Hello, world!");//println! is a macro that prints the string "Hello, world!" to the console, followed by a newline character. and println is a function that takes a string as an argument and prints it to the console. The exclamation mark (!) indicates that println is a macro, which is a special kind of function in Rust that can take a variable number of arguments and perform code generation at compile time.
}
// rustc main.rs - compiles the Rust code in main.rs into an executable file named main (or main.exe on Windows). You can then run the executable by ".\main" to see the output "Hello, world!" printed to the console.

//On windows, we see three files and one of them is a file containing debugging information with the .pdb extension. 